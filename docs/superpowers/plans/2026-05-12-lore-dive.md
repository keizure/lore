# lore-dive Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build `.claude/skills/lore-dive/SKILL.md`，让用户能在 Claude Code 中对任意根问题进行深度对话探索，实时记录到 session 文件，结束时生成双层文档（合成文章 + 探索路径附录）。

**Architecture:** 纯 prompt skill，无代码。遵循 writing-skills TDD 流程：RED（跑无 skill 基准测试）→ GREEN（写最小化 skill）→ REFACTOR（补漏洞）。

**Tech Stack:** Markdown skill (YAML frontmatter)，与 `.claude/skills/lore/SKILL.md` 同格式。

---

## 文件清单

| 操作 | 路径 | 说明 |
|------|------|------|
| Create | `.claude/skills/lore-dive/SKILL.md` | skill 主文件 |
| Create | `explore/.gitkeep` | 建立目录，供 skill 运行时写入 |

---

## Task 1：建立目录结构

**Files:**
- Create: `explore/.gitkeep`
- Create: `.claude/skills/lore-dive/` (目录)

- [ ] **Step 1: 创建 explore/ 目录**

```bash
mkdir -p /Users/liangjinrun/keisure/notebook/lore/explore
touch /Users/liangjinrun/keisure/notebook/lore/explore/.gitkeep
```

Expected: 目录创建成功，无报错。

- [ ] **Step 2: 创建 skill 目录**

```bash
mkdir -p /Users/liangjinrun/keisure/notebook/lore/.claude/skills/lore-dive
```

Expected: 目录创建成功。

- [ ] **Step 3: Commit**

```bash
git add explore/.gitkeep
git commit -m "chore: add explore/ directory for lore-dive sessions"
```

---

## Task 2：RED — 跑无 skill 的基准测试

目的：观察 AI 在没有任何 skill 指导时，面对「开始 lore-dive 探索」请求会做什么，记录偏差。

**Files:**
- Read: `.claude/skills/lore/SKILL.md`（参考格式）

- [ ] **Step 1: 用 Agent tool 分派基准测试 subagent**

使用以下 prompt 分派一个 general-purpose subagent（**不加载任何 skill**）：

```
你正在帮用户进行一次深度探索。用户说：

"我想深入理解 Transformer 的 Attention 机制是怎么工作的，帮我探索一下。"

请开始这个探索 session。探索两轮后，用户说「好了，结束吧」。
请完成整个流程。

工作目录：/Users/liangjinrun/keisure/notebook/lore
```

- [ ] **Step 2: 记录基准失败行为**

观察 subagent 的实际行为，预期会出现以下偏差（逐条确认）：

- [ ] 没有扫描 `explore/` 查找已有 session
- [ ] 没有创建 `explore/<slug>-session.md`
- [ ] 没有在每轮回答后写入文件
- [ ] 说「结束」时没有生成 `explore/<slug>.md`
- [ ] 没有生成双层结构（合成文章 + 附录）

将实际观察到的偏差记录下来（作为后续 skill 编写的依据）。

---

## Task 3：GREEN — 编写 SKILL.md

**Files:**
- Create: `.claude/skills/lore-dive/SKILL.md`

- [ ] **Step 1: 创建 SKILL.md，写入以下完整内容**

```markdown
---
name: lore-dive
description: Use when the user wants to deeply explore a question through open-ended conversation and capture the full exploration as a structured document — triggered by a question, topic, or concept to investigate.
---

# lore-dive — 深度探索笔记

## Overview

对用户的根问题进行深度对话探索，每轮问答实时写入 session 文件，探索结束后生成双层文档：合成文章（可直接阅读）+ 探索路径附录（保留完整问答过程）。

## When to Use

- 用户提出一个想深入探索的问题或话题，需要系统性记录
- 用户想通过对话一步步挖掘某个概念

**不适用：** 用户只是问一个简单问题，没有「把探索过程沉淀成文档」的意图。

## Workflow

### 阶段 0：Session Recovery（每次启动必须先执行）

1. 用 Glob 工具扫描 `explore/*-session.md`
2. 用 Read 工具读取找到的文件，检查 frontmatter 中 `status` 字段
3. **找到 `status: active` 的 session：**
   - 读取完整内容（恢复上下文）
   - 告知用户：「发现进行中的探索：『[root]』，继续这个 session 吗？还是开启新探索？」
   - 用户选「继续」→ 跳至阶段 2，从上次中断处继续
   - 用户选「新开」→ 进入阶段 1
4. **未找到活跃 session：** 直接进入阶段 1

**关键：session 文件是唯一的跨会话记忆。每次启动必须执行，不可跳过，即使用户提供了新问题。**

---

### 阶段 1：新建 Session

1. 从根问题推导 slug（小写英文，空格和特殊字符替换为 `-`，只保留字母/数字/连字符）
   - 「如果让你来做整个大语言模型的分层，你会怎么分？」→ `llm-layering`
   - 「Transformer 的 Attention 机制是怎么工作的」→ `transformer-attention`

2. 用 Write 工具创建 `explore/<slug>-session.md`：

```
---
root: "<用户的根问题>"
started: <YYYY-MM-DDThh:mm:ss>
status: active
---
```

3. 立即进入阶段 2，回答根问题。

---

### 阶段 2：探索循环（每轮重复）

每轮：

1. **回答用户的问题**（充分、深入）

2. **立即用 Edit 工具追加写入 session 文件**（回答后立即执行，不可积攒）：

```
## Q<n> <简短标题>

**问：** <用户原文>

**答：** <完整回答>
```

3. **判断是否需要分支提示：** 如果本轮回答引出了值得独立深挖的子问题，且用户尚未问到，在答案末尾追加一行，同时写入文件：
   > 💡 **未探索分支：** <简短描述>

   不强制用户跟进，用户可直接忽略继续提问。

4. **检测话题漂移：** 如果用户新问题与根问题语义距离较远，在回答前先提示：
   > 「这个问题和当前 session『[root]』关联不大，要新开一个 session 吗？还是继续归入当前？」

   用户选「新开」→ 用 Edit 将当前 session `status` 改为 `paused`，回到阶段 1。

5. **检测完成意图（每轮检查）：** 用户说「够了」「结束」「可以了」「生成文档」「好了」「done」→ 立即进入阶段 3。

---

### 阶段 3：生成双层文档

1. 用 Read 工具读取完整 session 文件
2. 将所有问答合成为连贯文章（去掉「问：/答：」格式，直接可读，保留核心内容）
3. 用 Write 工具生成 `explore/<slug>.md`：

```
---
root: "<根问题>"
explored: <YYYY-MM-DD>
---

# <合成标题>

<合成文章正文，连贯可读，中文>

---

## 附录：探索路径

### Q1 — <标题>

**问：** ...

**答：** ...

### Q2 — <标题>

...
```

4. 用 Edit 工具将 session 文件 `status: active` 改为 `status: done`
5. 告知用户：「探索文档已生成：`explore/<slug>.md`」

---

## Constraints

- 每轮问答后**立即写入** session 文件，不可积攒到对话结束
- 只写入 `explore/` 目录，不修改其他文件
- 合成文章使用**中文**，连贯可读
- 每次启动**必须先执行 Session Recovery**，不可跳过
- 分支提示只在 AI 主动判断有必要时出现，不强制每轮都加
```

- [ ] **Step 2: 验证文件存在且格式正确**

```bash
head -5 /Users/liangjinrun/keisure/notebook/lore/.claude/skills/lore-dive/SKILL.md
```

Expected: 输出 YAML frontmatter 头部，`name: lore-dive`。

- [ ] **Step 3: Commit**

```bash
git add .claude/skills/lore-dive/SKILL.md
git commit -m "feat: add lore-dive skill (GREEN - initial implementation)"
```

---

## Task 4：GREEN — 跑验证测试

目的：用同一场景跑 subagent，这次加载 skill，验证关键行为是否符合预期。

- [ ] **Step 1: 分派验证 subagent**

使用以下 prompt 分派 general-purpose subagent（**加载 skill**）：

```
请先调用 lore-dive skill，然后按照 skill 的指引帮用户完成以下操作：

用户说：「我想深入理解 Transformer 的 Attention 机制是怎么工作的，帮我探索一下。」

探索两轮（每轮一个问题）后，用户说「好了，结束吧」。
请完成整个流程。

工作目录：/Users/liangjinrun/keisure/notebook/lore
```

- [ ] **Step 2: 验证关键行为（逐条对照）**

- [ ] 启动时执行了 Session Recovery（调用了 Glob `explore/*-session.md`）
- [ ] 创建了 `explore/transformer-attention-session.md`
- [ ] 每轮回答后立即追加写入 session 文件
- [ ] 用户说「结束」后生成了 `explore/transformer-attention.md`
- [ ] 最终文档有主体（合成文章）和附录（探索路径）两部分
- [ ] Session 文件 `status` 改为 `done`

- [ ] **Step 3: 验证生成的文件**

```bash
ls /Users/liangjinrun/keisure/notebook/lore/explore/
cat /Users/liangjinrun/keisure/notebook/lore/explore/transformer-attention.md | head -30
```

Expected: 看到 `transformer-attention-session.md`（status: done）和 `transformer-attention.md`，后者有 frontmatter、正文、`## 附录：探索路径` 三部分。

---

## Task 5：REFACTOR — 补漏洞（如有）

根据 Task 4 的观察结果，如果发现新的行为偏差，在此任务中修复 SKILL.md。

**常见漏洞模式（如出现，针对性修复）：**

| 观察到的偏差 | SKILL.md 修复位置 |
|---|---|
| 忘记 Session Recovery | 在 Overview 后加粗警告 |
| 分支提示出现过于频繁 | 在约束中加「每次对话最多 1 个分支提示」 |
| 话题漂移误判（把相关问题当成漂移） | 在漂移检测规则中加「同一领域的深入问题不算漂移」 |
| 合成文章太短/太浅 | 在阶段 3 中加「合成文章篇幅应与探索深度匹配，不少于 300 字」 |

- [ ] **Step 1: 根据测试结果修改 SKILL.md（如有需要）**

修改后重新跑一次 Task 4 的 subagent 验证，确认修复有效。

- [ ] **Step 2: Commit（如有修改）**

```bash
git add .claude/skills/lore-dive/SKILL.md
git commit -m "refactor: close lore-dive skill loopholes found in verification"
```

---

## Task 6：跨会话恢复测试

目的：验证 Session Recovery 在真正跨会话的场景下能正常工作。

- [ ] **Step 1: 手动创建一个模拟的半完成 session**

```bash
cat > /Users/liangjinrun/keisure/notebook/lore/explore/test-recovery-session.md << 'EOF'
---
root: "Python 的 GIL 到底锁的是什么？"
started: 2026-05-12T09:00:00
status: active
---

## Q1 根问题

**问：** Python 的 GIL 到底锁的是什么？

**答：** GIL（Global Interpreter Lock）是 CPython 中的一个互斥锁，它保证同一时刻只有一个线程在执行 Python 字节码...
EOF
```

- [ ] **Step 2: 分派 subagent，不提供根问题，只说「我想继续探索」**

```
请先调用 lore-dive skill，然后帮用户继续之前的探索。

用户说：「我想继续之前的探索」（没有提供新问题）

工作目录：/Users/liangjinrun/keisure/notebook/lore
```

- [ ] **Step 3: 验证 subagent 行为**

- [ ] 识别到 `test-recovery-session.md` 中的活跃 session
- [ ] 告知用户发现了「Python 的 GIL 到底锁的是什么？」的未完成探索
- [ ] 提示用户选择继续或新开

- [ ] **Step 4: 清理测试文件**

```bash
rm /Users/liangjinrun/keisure/notebook/lore/explore/test-recovery-session.md
```

---

## Task 7：最终 Commit

- [ ] **Step 1: 检查所有文件状态**

```bash
git status
git log --oneline -5
```

- [ ] **Step 2: 最终 commit（如有未提交内容）**

```bash
git add -A
git commit -m "feat: complete lore-dive skill with session recovery and dual-layer output"
```
